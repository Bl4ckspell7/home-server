- name: Configure base system
  hosts: svr1
  roles:
    - role: apt
      tags: apt

    - role: unattended-upgrades
      tags: unattended-upgrades
      vars:
        # No scheduled reboot: the evening power-off applies the new kernel. The
        # timer covers longer continuous runs, so the upgrade window sits just
        # before it.
        uu_automatic_reboot: false
        uu_reboot_timer: true
        uu_apt_daily_oncalendar: "*-*-* 03:30"
        uu_apt_daily_upgrade_oncalendar: "*-*-* 04:00"
        uu_apt_daily_upgrade_delay: "20m"

    - role: bash
      tags: bash

    - role: sysctl
      tags: sysctl

    - role: flatpak
      tags: flatpak

    - role: grub
      tags: grub

    - role: locale
      tags: locale

    - role: ssh
      tags: ssh

    - role: ufw
      tags: ufw

    - role: luks
      tags: luks

    - role: suspend
      tags: suspend

    - role: gtk
      tags: gtk

    - role: wol
      tags: wol
      vars:
        wol_interface: enp5s0

    - role: dns
      tags: dns

    - role: wireguard
      tags: wireguard

    - role: docker
      tags: docker
